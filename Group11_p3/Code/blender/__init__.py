# blender package
# Note: bpy (Blender Python API) is only available when running inside Blender.
# Do not import bpy at the package level — import it inside functions/classes.
    # if we did have bpy here, then when not running inside Blender and importing anything from the blender packet, everything would break.
