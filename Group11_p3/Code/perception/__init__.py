# perception package
# this tells python that the directory perception is a package, not just a folder
# having this be a package allows for: from perception.objects import ObjectDetector (import the ObjectDetector class)